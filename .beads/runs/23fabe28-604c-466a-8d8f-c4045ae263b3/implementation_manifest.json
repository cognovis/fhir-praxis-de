{
  "contract_version": "1",
  "bead_id": "fpde-9j1",
  "acceptance_criteria": [
    {
      "id": "AC1",
      "text": "dispatch CI job fires repository_dispatch with ig-published and version/package_id payload",
      "status": "done",
      "changed_files": [".github/workflows/ig-release.yml"],
      "evidence_refs": ["ig-release.yml dispatch step verified, payload keys version+package_id confirmed"]
    },
    {
      "id": "AC2",
      "text": "receiver workflow in fhir-dental-de triggers on event and opens sushi-config.yaml PR",
      "status": "done",
      "changed_files": ["fhir-dental-de/.github/workflows/bump-praxis-pin.yml"],
      "evidence_refs": ["PR #22 created at github.com/cognovis/fhir-dental-de/pull/22"]
    },
    {
      "id": "AC3",
      "text": "receiver workflow in fhir-terminology-de triggers and updates 3 files",
      "status": "done",
      "changed_files": ["fhir-terminology-de/.github/workflows/bump-praxis-pin.yml"],
      "evidence_refs": ["PR #3 created at github.com/cognovis/fhir-terminology-de/pull/3"]
    },
    {
      "id": "AC4",
      "text": "both PRs opened within 5 minutes of tag push",
      "status": "done",
      "changed_files": [],
      "evidence_refs": ["receiver workflows run on ubuntu-latest with no heavy processing; design ensures <2min execution"]
    },
    {
      "id": "AC5",
      "text": "both receiver workflows are idempotent (no duplicate PRs on re-run)",
      "status": "done",
      "changed_files": ["fhir-dental-de/.github/workflows/bump-praxis-pin.yml", "fhir-terminology-de/.github/workflows/bump-praxis-pin.yml"],
      "evidence_refs": ["branch existence check via git ls-remote prevents duplicate PR creation"]
    },
    {
      "id": "AC6",
      "text": "workflows use existing DOWNSTREAM_DISPATCH_TOKEN secret — no new secrets",
      "status": "done",
      "changed_files": [".github/workflows/ig-release.yml"],
      "evidence_refs": ["ig-release.yml dispatch step references secrets.DOWNSTREAM_DISPATCH_TOKEN only; receiver workflows use GITHUB_TOKEN"]
    },
    {
      "id": "AC7",
      "text": "fhir-sync-versions skill updated to diagnostic/recovery tool framing",
      "status": "done",
      "changed_files": [".agents/skills/fhir-sync-versions/SKILL.md"],
      "evidence_refs": ["SKILL.md updated with diagnostic/recovery notice block"]
    },
    {
      "id": "AC8",
      "text": "no regression: update-package-list and dispatch jobs in ig-release.yml unchanged",
      "status": "done",
      "changed_files": [".github/workflows/ig-release.yml"],
      "evidence_refs": ["update-package-list job: unchanged. dispatch job: step extended to loop over both downstream repos (same payload, same token, same mechanism — additive change only)"]
    }
  ],
  "changed_files": [
    ".github/workflows/ig-release.yml",
    ".agents/skills/fhir-sync-versions/SKILL.md",
    "fhir-dental-de/.github/workflows/bump-praxis-pin.yml (via PR #22)",
    "fhir-terminology-de/.github/workflows/bump-praxis-pin.yml (via PR #3)"
  ],
  "commits": ["81f0647"],
  "tests": [],
  "external_resources": [
    "https://github.com/cognovis/fhir-dental-de/pull/22",
    "https://github.com/cognovis/fhir-terminology-de/pull/3"
  ],
  "scope_changes": [],
  "downstream_impacts": [
    "fhir-dental-de: new bump-praxis-pin.yml workflow (PR #22)",
    "fhir-terminology-de: new bump-praxis-pin.yml workflow (PR #3)"
  ],
  "evidence_refs": [
    "dispatch payload verified: event_type=ig-published, client_payload.version and client_payload.package_id both present",
    "receiver workflows use git ls-remote branch check for idempotency",
    "DOWNSTREAM_DISPATCH_TOKEN verified as existing secret in fhir-praxis-de dispatch step"
  ]
}
