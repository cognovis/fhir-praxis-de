node_modules/
output/
fsh-generated/
dist/
temp/
template/
*.tgz
.DS_Store
input-cache/
txCache/
.fhir-publish/
.fhir-publish.yml
__pycache__/
*.py[cod]

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Claude session logs
logs/

# Claude Code project config (keep commands/ tracked)
.claude/*
!.claude/commands/

# Beads task tracker — internal, not part of the IG.
# Data lives on the Dolt remote; the local working copy stays untracked.
.beads/

# Local Aidbox / IG Publisher config
config.yaml

# IG Publisher artifacts in root
Requirements-fromNarrative.json

# session-close transient artifacts (per-worktree IPC files)
.session-close.lock
.worktree-handoff.json
.wave-orchestrator.lock

# playwright-cli local cache
.playwright-cli/

# Non-IG tooling — kept locally but not tracked on the public IG repo.
# Agent/skill tooling, release orchestration (moved to fhir-management),
# and local dev convenience files.
.agents/
.library.lock
AGENTS.md
cliff.toml
fhir-versions.lock.yaml
docker-compose.yaml
init-bundle.json
